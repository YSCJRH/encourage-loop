# Future app-server design

A later advanced version can use Codex app-server to stream turn events, steer active turns, or interrupt obvious drift.

This is intentionally out of MVP scope.

The app-server path should be considered only after the CLI + skill loop is trusted.
