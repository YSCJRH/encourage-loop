# EncourageLoop harness

This harness checks the boring but important pieces:

- CLI smoke commands run.
- fake-done validation fails when expected.
- handoff generation works.
- tests pass.

Run:

```bash
node harness/scripts/run-harness.js
```
