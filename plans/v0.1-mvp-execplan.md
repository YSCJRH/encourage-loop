# EncourageLoop v0.1 MVP ExecPlan

## Summary

Build the smallest useful EncourageLoop package: a deterministic Node.js CLI, a Codex skill,
a plugin manifest, repo-scoped skill loading, local marketplace metadata, and blueprints/prompts
that let Codex App continue work without re-planning the entire project each turn.

This plan is the execution contract. Older research and blueprints are boundary context, not the per-turn script.

## Execution Cursor

- Current baseline: new open-source repository at `https://github.com/YSCJRH/encourage-loop`
- Target: v0.1.0 MVP package
- Current stage: R4
- Stage status: release_candidate
- Last completed evidence: remote GitHub Actions CI run `24993879706` passed for commit
  `154e8f6` after release-candidate hardening.
- Last validation: `npm ci`, `npm test`, `npm run lint`, `node harness/scripts/run-harness.js`,
  `npm pack --dry-run`, `node bin/encourage.js validate`, and remote CI pass.
- Next atomic task: maintainer explicitly decides whether to manually tag, publish, or release.
- Known blockers: none
- Scope guard:
  - no daemon
  - no keyboard injection
  - no LLM calls
  - no automatic merge/release
  - no abuse/whip branding

## Phase R0 — Repository bootstrap and project pact

Deliverables:

- [x] README.md
- [x] AGENTS.md
- [x] LICENSE
- [x] package.json
- [x] `.gitignore`
- [x] Codex startup prompts

Acceptance:

- [x] README explains what EncourageLoop is and is not.
- [x] AGENTS.md defines non-negotiable principles and report format.
- [x] `npm test` runs.

## Phase R1 — CLI cursor MVP

Deliverables:

- [x] `encourage init --plan <path>`
- [x] `encourage status [--json]`
- [x] `encourage checkpoint`
- [x] `encourage validate`
- [x] `encourage nudge`
- [x] `encourage handoff`

Acceptance:

- [x] Commands work in a temp directory.
- [x] `validate` catches `complete` with no validation.
- [x] `handoff` writes `.encourage/handoff.md`.

## Phase R2 — Codex skill and plugin packaging

Deliverables:

- [x] `skills/encourage-loop/SKILL.md`
- [x] `.agents/skills/encourage-loop/SKILL.md`
- [x] `.codex-plugin/plugin.json`
- [x] `.agents/plugins/marketplace.json`

Acceptance:

- [x] Codex can use the repo-scoped skill directly.
- [x] Plugin manifest is valid JSON and points to `./skills/`.

## Phase R3 — Harness and dogfood examples

Deliverables:

- [x] `harness/scripts/run-harness.js`
- [x] example plan/cursor fixtures
- [x] WinChronicle dogfood plan
- [x] drift and done-gate scorecards

Acceptance:

- [x] harness script runs tests and smoke commands.
- [x] examples are realistic enough for Codex to use as fixtures.

## Phase R4 — Future integrations specification

Deliverables:

- [x] hooks example
- [x] app-server notes
- [x] MCP future plan

Acceptance:

- [x] Future features are documented as non-MVP.
- [x] No future feature is accidentally implemented in MVP.

## Current validation evidence

- `npm ci`: passes with the checked-in lockfile.
- `npm test`: passes with 27 CLI, cursor, validate, handoff, nudge, packaging, CI, and docs
  contract tests.
- `npm run lint`: passes syntax checks for the CLI, source modules, and harness script.
- `node harness/scripts/run-harness.js`: passes tests plus init/status/nudge/validate/handoff smoke
  flow in a temp directory.
- `npm pack --dry-run`: passes without publishing; package contents are explicitly controlled by
  `package.json` `files`, with 59 files and an approximately 25 kB tarball.
- `node bin/encourage.js validate`: passes for this repository cursor with no warnings.
- GitHub Actions run `24993879706`: passed on `main` for commit `154e8f6`.
- `docs/v0.1-readiness-review.md`: summarizes changed files, validation evidence, package contents,
  remaining manual release steps, and release blockers.

## Remaining v0.1 release-readiness gaps

- No manual release, tag, npm publish, PR merge, or GitHub release has been performed.
- Release policy remains manual confirm.

## Test Plan

- `npm test`
- `npm ci`
- `npm run lint`
- `node harness/scripts/run-harness.js`
- `npm pack --dry-run`
- `node bin/encourage.js init --plan plans/v0.1-mvp-execplan.md --force`
- `node bin/encourage.js status`
- `node bin/encourage.js validate`
- `node bin/encourage.js nudge`
- `node bin/encourage.js handoff`

## Release policy

Manual confirm. Do not automatically merge, tag, publish, or release.
