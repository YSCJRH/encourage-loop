# EncourageLoop v0.1.1 Post-Release Hardening ExecPlan

## Summary

Harden the released MVP before adding any runtime scope. This round turns the lessons from the
v0.1.0 release into deterministic contracts, fixtures, tests, and docs.

The goal is not to add daemon, MCP, app-server, hooks runtime, UI, or automation. The goal is to
make future release and handoff work less fragile while keeping the CLI deterministic and boring.

## Execution Cursor

- Current baseline: `v0.1.0` is published to npm and GitHub release.
- Target: v0.1.1 post-release hardening candidate.
- Current stage: R0
- Stage status: in_progress
- Last completed evidence: v0.1.0 release completed; `origin/main` has release completion docs.
- Last validation: `npm test`, `npm run lint`, `node harness/scripts/run-harness.js`,
  `npm pack --dry-run`, `node bin/encourage.js validate`, and CI pass for the release-completion
  commit.
- Next atomic task: add release lifecycle fixtures for blocked publish, tag retarget, npm
  published, GitHub release published, and released cursor states.
- Known blockers: none.
- Scope guard:
  - no daemon
  - no keyboard injection
  - no LLM calls
  - no MCP server implementation
  - no app-server or desktop overlay
  - no automatic merge or release
  - no network calls in runtime behavior
  - no abuse/whip branding

## Phase R0 — Release lifecycle fixtures and scorecard

Deliverables:

- [ ] Fixture cursor/state examples for release-candidate, blocked publish, tag-retarget, npm
  published, GitHub release published, and released states.
- [ ] Release lifecycle scorecard that names expected evidence and stop conditions.
- [ ] Docs note explaining partial-release states without prescribing destructive cleanup.

Acceptance:

- [ ] Fixtures are deterministic and do not require network access.
- [ ] Scorecard distinguishes release-candidate, blocked, partially released, and released.
- [ ] Partial release guidance says to record facts and require explicit approval before any
  corrective tag or release action.

## Phase R1 — Cursor and blocker hygiene

Deliverables:

- [ ] Cursor/checkpoint behavior for clearing resolved blockers explicitly and safely.
- [ ] Tests for repeated blockers and released-state handoff readability.
- [ ] Documentation for when blockers should remain historical evidence versus current blockers.

Acceptance:

- [ ] Released status can show no current blockers while preserving historical evidence.
- [ ] Handoff stays readable after long release histories.
- [ ] Existing cursor Markdown fallback remains compatible.

## Phase R2 — Packaging and install smoke hardening

Deliverables:

- [ ] Local tarball install smoke for the `encourage` binary using `npm pack` output in a temp
  directory.
- [ ] Packaging tests for package bin, package files allowlist, and no publish-time manifest
  normalization.
- [ ] Harness wiring for the install smoke if it remains deterministic and local-only.

Acceptance:

- [ ] The packed tarball installs locally and exposes `encourage`.
- [ ] The smoke test does not call the npm registry.
- [ ] Publish dry-run guidance notes that it must run only after a future version bump, because
  `0.1.0` is already published.

## Phase R3 — Release checklist and readiness docs cleanup

Deliverables:

- [ ] Release checklist reflects the actual v0.1.0 path: 2FA, tag retarget approval, npm publish,
  GitHub release, and post-release docs.
- [ ] Readiness review separates pre-release readiness from completed release evidence.
- [ ] Startup and execution prompts point post-release work toward a new plan first.

Acceptance:

- [ ] Docs keep release actions manual-only.
- [ ] Docs do not suggest force or destructive recovery without explicit approval.
- [ ] Future integrations remain documented as non-MVP and non-default.

## Phase R4 — v0.1.1 readiness gate

Deliverables:

- [ ] Reconcile this plan with completed evidence.
- [ ] Run full local validation and package dry-run.
- [ ] Prepare a concise v0.1.1 readiness note without publishing.

Acceptance:

- [ ] `npm test` passes.
- [ ] `npm run lint` passes.
- [ ] `node harness/scripts/run-harness.js` passes.
- [ ] `npm pack --dry-run` passes.
- [ ] `node bin/encourage.js validate` passes.
- [ ] No tag, npm publish, GitHub release, merge, or deployment is performed without explicit
  maintainer confirmation.

## Test Plan

- `npm test`
- `npm run lint`
- `node harness/scripts/run-harness.js`
- `npm pack --dry-run`
- `npm publish --dry-run` only after a future version bump
- `node bin/encourage.js status`
- `node bin/encourage.js validate`
- Targeted fixture and install-smoke tests added during this round

## Non-goals

- No daemon.
- No MCP server implementation.
- No app-server integration.
- No desktop overlay.
- No keyboard injection.
- No LLM judge or LLM calls.
- No cloud sync.
- No automatic merge, tag, publish, release, or destructive repair.
- No network calls in runtime behavior.

## Release policy

Manual confirm. Do not automatically merge, tag, publish, or release. This plan may prepare a
v0.1.1 candidate, but release actions require a separate explicit maintainer decision.
