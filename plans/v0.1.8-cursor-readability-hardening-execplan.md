# EncourageLoop v0.1.8 Cursor Readability Hardening ExecPlan

## Summary

After the v0.1.6 and v0.1.7 maintenance rounds, `node bin/encourage.js status` and release
recovery docs are bounded and readable, but `.encourage/cursor.md` itself can still become very
large during long-running work because it renders the full evidence and validation arrays.

The goal is to make the human-readable cursor markdown stay concise without deleting evidence or
weakening machine-readable state. Keep the full structured history in `.encourage/cursor.json` and
state JSONL files. Do not rewrite historical `.encourage/` logs, add a daemon, add network runtime
behavior, add release automation, or perform any release action.

## Execution Cursor

- Current baseline: v0.1.6 is released; v0.1.7 release recovery hardening is complete on `main`.
- Target: v0.1.8 cursor readability hardening candidate.
- Current stage: R1
- Stage status: in_progress
- Last completed evidence: R0 cursor readability coverage and implementation are complete, and
  full local validation plus package dry-run passed for the cursor readability change.
- Last validation: `npm test` passed with 58 tests; `npm run lint`, `node
  harness/scripts/run-harness.js`, `npm pack --dry-run`, and `node bin/encourage.js validate`
  passed.
- Next atomic task: commit and push the cursor-readability change, then record GitHub Actions
  evidence.
- Known blockers: none.
- Scope guard:
  - no daemon
  - no keyboard injection
  - no LLM calls
  - no MCP server implementation
  - no app-server or desktop overlay
  - no automatic merge or release
  - no package publish
  - no GitHub release creation
  - no tag deletion, tag retarget, or force-push
  - no version bump
  - no runtime network calls

## Phase R0 — Cursor Markdown Readability Contract

Deliverables:

- [x] Add tests showing cursor markdown stays readable when evidence and validation histories are
  long.
- [x] Preserve full evidence and validation arrays in `.encourage/cursor.json`.
- [x] Make cursor markdown show recent evidence and validation entries with a count of hidden
  older entries.
- [x] Keep plan, stage, status, next task, known blockers, scope guard, and updated timestamp
  visible in cursor markdown.
- [x] Define markdown fallback behavior when `.encourage/cursor.json` is missing.

Acceptance:

- [x] Long cursor histories no longer render as unbounded markdown lists.
- [x] Cursor markdown says older entries remain in `.encourage/` history when entries are hidden.
- [x] JSON consumers still receive full `last_completed_evidence` and `last_validation` arrays.
- [x] Markdown fallback recovers visible recent entries and does not fabricate hidden entries.
- [x] No `.encourage/` history is deleted or rewritten.

## Phase R1 — Evidence

Deliverables:

- [x] Run full local validation.
- [x] Run package dry-run.
- [ ] Commit and push the cursor-readability change.
- [ ] Record GitHub Actions evidence after push.

Acceptance:

- [x] `npm test` passes.
- [x] `npm run lint` passes.
- [x] `node harness/scripts/run-harness.js` passes.
- [x] `npm pack --dry-run` passes.
- [x] `node bin/encourage.js validate` passes.
- [ ] GitHub Actions passes on the cursor-readability commit.
- [ ] No tag, npm publish, GitHub release, merge, deployment, or version bump is performed.

## Test Plan

- `npm test`
- `npm run lint`
- `node harness/scripts/run-harness.js`
- `npm pack --dry-run`
- `node bin/encourage.js status`
- `node bin/encourage.js validate`
- Targeted `tests/cursor.test.js` coverage for long cursor histories

## Non-goals

- No version bump.
- No npm publish.
- No tag or GitHub release.
- No tag deletion, tag retarget, or force-push.
- No daemon.
- No MCP server implementation.
- No app-server integration.
- No desktop overlay.
- No keyboard injection.
- No LLM judge or LLM calls.
- No cloud sync.
- No runtime network calls.

## Release Policy

Manual confirm. This plan may improve cursor readability, but it does not authorize any release
action.
