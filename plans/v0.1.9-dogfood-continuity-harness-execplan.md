# EncourageLoop v0.1.9 Dogfood Continuity Harness ExecPlan

## Summary

After v0.1.8, the cursor, text status, and handoff surfaces stay readable during long work. The
next maintenance round turns the WinChronicle dogfood blueprint into deterministic continuity
coverage so EncourageLoop proves the core loop on a realistic long-running, phase-based project.

The goal is dogfood continuity hardening, not new runtime scope. Keep the current cursor and
referenced exec plan as the execution contract; treat older blueprints as north-star context only
when scope, privacy, or inconsistency questions require them.

## Execution Cursor

- Current baseline: v0.1.8 cursor readability hardening is complete on `main` at
  `1dd35ed4d81890589fc535935b48516c6739301c`.
- Target: v0.1.9 dogfood continuity harness candidate.
- Current stage: R3
- Stage status: in_progress
- Last completed evidence: R0-R2 dogfood contract, operator guidance, deterministic tests, full
  local validation, and package dry-run are complete.
- Last validation: targeted dogfood tests passed with 33 tests; `npm test` passed with 61 tests;
  `npm run lint`, `node harness/scripts/run-harness.js`, `npm pack --dry-run`, and `node
  bin/encourage.js validate` passed.
- Next atomic task: commit and push the dogfood continuity harness change, then record GitHub
  Actions evidence.
- Known blockers: none.
- Scope guard:
  - no daemon
  - no keyboard injection
  - no LLM calls
  - no MCP server implementation
  - no app-server or desktop overlay
  - no hooks runtime
  - no UI
  - no automatic merge or release
  - no package publish
  - no GitHub release creation
  - no tag deletion, tag retarget, or force-push
  - no version bump
  - no runtime network calls

## Phase R0 - Dogfood Contract

Deliverables:

- [x] Add deterministic dogfood fixture inventory for the WinChronicle continuity scenario.
- [x] Add dogfood scorecard criteria for no phase jump, no blueprint re-planning, no fake done,
  and scope guard preservation.
- [x] Record v0.1.9 as an unreleased repository maintenance candidate.

Acceptance:

- [x] Dogfood fixture identifies the current cursor and phased plan as the execution contract.
- [x] Dogfood fixture identifies the long-term blueprint as north-star context only.
- [x] Scorecard states expected behavior for phase drift, blueprint drift, fake completion, and
  scope guard conflicts.
- [x] No release, runtime integration, or version bump is performed.

## Phase R1 - Operator Guidance

Deliverables:

- [x] Update handoff output so continuation prompts follow `.encourage/cursor.*` and the current
  exec plan before older blueprints.
- [x] Update nudge output so the next atomic task remains central and older blueprints remain
  background context.
- [x] Update skill and prompt guidance with the same blueprint-vs-plan rule.

Acceptance:

- [x] Handoff and nudge guidance do not ask the agent to re-plan from the long-term blueprint when
  the cursor and current plan are consistent.
- [x] Guidance still tells the agent to validate before claiming completion.
- [x] Packaged and repo-scoped skills stay synchronized.

## Phase R2 - Deterministic Tests

Deliverables:

- [x] Add docs-contract coverage for the dogfood continuity contract.
- [x] Add handoff and nudge tests for cursor/current-plan-first guidance.
- [x] Add packaging tests for dogfood fixture, scorecard, and plan inclusion.
- [x] Keep existing validation gate coverage for missing plan, complete-without-validation, and
  dangerous command history.

Acceptance:

- [x] Targeted dogfood guidance and packaging tests pass.
- [x] Existing validation behavior remains unchanged.
- [x] Future integrations remain documented-only.

## Phase R3 - Evidence

Deliverables:

- [x] Run full local validation.
- [x] Run package dry-run.
- [x] Checkpoint v0.1.9 evidence.
- [ ] Commit and push the dogfood continuity harness change.
- [ ] Record GitHub Actions evidence after push.

Acceptance:

- [x] `npm test` passes.
- [x] `npm run lint` passes.
- [x] `node harness/scripts/run-harness.js` passes.
- [x] `npm pack --dry-run` passes.
- [x] `node bin/encourage.js validate` passes.
- [ ] GitHub Actions passes on the dogfood continuity harness commit.
- [ ] No tag, npm publish, GitHub release, merge, deployment, or version bump is performed.

## Test Plan

- Targeted: `node --test --test-reporter=spec tests/docs-contract.test.js tests/handoff.test.js
  tests/nudge.test.js tests/packaging.test.js`
- Full: `npm test`
- `npm run lint`
- `node harness/scripts/run-harness.js`
- `npm pack --dry-run`
- `node bin/encourage.js status`
- `node bin/encourage.js validate`

## Non-goals

- No version bump.
- No npm publish.
- No tag or GitHub release.
- No tag deletion, tag retarget, or force-push.
- No daemon.
- No MCP server implementation.
- No app-server integration.
- No desktop overlay.
- No hooks runtime.
- No UI.
- No keyboard injection.
- No LLM judge or LLM calls.
- No cloud sync.
- No runtime network calls.

## Release Policy

Manual confirm. This plan may improve dogfood continuity coverage, but it does not authorize any
release action.
