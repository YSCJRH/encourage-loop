# EncourageLoop v0.1.7 Release Recovery Hardening ExecPlan

## Summary

After the v0.1.6 release, the project hit a realistic partial-release path: the release-preparation
commit and `v0.1.6` tag were pushed, `npm publish` was blocked by npm authentication and
publish-time OTP, the maintainer completed npm publication outside Codex, and Codex then verified
and completed the GitHub release and post-release evidence.

The goal is to make that recovery path deterministic and well documented for future releases. This
is release-process hardening only. Do not add a daemon, MCP server, app server, desktop overlay,
keyboard injection, LLM judge, network runtime behavior, automatic publish tool, automatic tag
repair, automatic release creation, or automatic retry logic.

## Execution Cursor

- Current baseline: v0.1.6 is released, npm `encourage-loop@0.1.6` is published as `latest`, and
  GitHub release `v0.1.6` is published.
- Target: v0.1.7 release recovery hardening candidate.
- Current stage: R1
- Stage status: complete
- Last completed evidence: v0.1.6 post-release evidence was committed in
  `f58a4be68c912f95e3482c1137cdef1a99c2eea7` and GitHub Actions run `25194264397` passed;
  R0 added release recovery guidance and docs-contract coverage.
- Last validation: `node --test --test-reporter=spec tests/docs-contract.test.js` passed with
  16 tests after the recovery guidance update; `npm test`, `npm run lint`,
  `node harness/scripts/run-harness.js`, `npm pack --dry-run`, and
  `node bin/encourage.js validate` passed locally; GitHub Actions run `25213645122` passed for
  `f5add66062ab84ecf5e255c9230e9060b5475630`.
- Next atomic task: await maintainer direction for the next maintenance plan or release decision.
- Known blockers: none.
- Scope guard:
  - no daemon
  - no keyboard injection
  - no LLM calls
  - no MCP server implementation
  - no app-server or desktop overlay
  - no automatic merge or release
  - no package publish
  - no GitHub release creation
  - no tag deletion, tag retarget, or force-push
  - no version bump
  - no runtime network calls

## Phase R0 — Release Recovery Guidance

Deliverables:

- [x] Add docs-contract coverage for release recovery guidance.
- [x] Document how to distinguish npm login failure (`E401`) from publish-time OTP failure
  (`EOTP`).
- [x] Document the partial-release state where a tag exists before npm or GitHub release
  completion.
- [x] Document read-only verification commands before any retry.
- [x] Document safe stop points and required maintainer confirmation for corrective actions.

Acceptance:

- [x] Recovery guidance tells maintainers to verify npm versions, dist-tags, remote tags, and
  GitHub release state before retrying anything.
- [x] Recovery guidance forbids automatic tag deletion, tag retargeting, force-push, unpublish, or
  publish retries without explicit maintainer approval.
- [x] Recovery guidance keeps OTP handling manual and short-lived.
- [x] Tests pin the guidance so it does not drift into automation.
- [x] No release command is performed while preparing this guidance.

## Phase R1 — Evidence

Deliverables:

- [x] Run full local validation.
- [x] Run package dry-run.
- [x] Commit and push the release-recovery guidance.
- [x] Record GitHub Actions evidence after push.

Acceptance:

- [x] `npm test` passes.
- [x] `npm run lint` passes.
- [x] `node harness/scripts/run-harness.js` passes.
- [x] `npm pack --dry-run` passes.
- [x] `node bin/encourage.js validate` passes.
- [x] GitHub Actions passes on the guidance commit.
- [x] No tag, npm publish, GitHub release, merge, deployment, or version bump is performed.

## Test Plan

- `npm test`
- `npm run lint`
- `node harness/scripts/run-harness.js`
- `npm pack --dry-run`
- `node bin/encourage.js status`
- `node bin/encourage.js validate`
- Targeted docs-contract coverage for release recovery guidance

## Non-goals

- No version bump.
- No npm publish.
- No tag or GitHub release.
- No tag deletion, tag retarget, or force-push.
- No daemon.
- No MCP server implementation.
- No app-server integration.
- No desktop overlay.
- No keyboard injection.
- No LLM judge or LLM calls.
- No cloud sync.
- No runtime network calls.

## Release Policy

Manual confirm. This plan may improve release recovery documentation, but it does not authorize
any release action.
