# EncourageLoop v0.1.9 Release Preparation ExecPlan

## Summary

Close the current v0.1.9 maintenance line and prepare it as the next manual release baseline.

The maintainer selected the current dogfood continuity harness baseline for release preparation.
This plan authorizes release-preparation metadata and evidence only. It does not authorize tagging,
publishing to npm, creating a GitHub release, force-pushing, retargeting tags, or destructive
release recovery.

## Execution Cursor

- Current baseline: v0.1.9 dogfood continuity harness is complete on `main` at
  `14e423220706f2a99a0ebf485cf8e86067cb2303`.
- Target: v0.1.9 release candidate.
- Current stage: R1
- Stage status: complete
- Last completed evidence: R0 prepared `0.1.9` package metadata, `docs/v0.1.9-release-notes.md`,
  project-status updates, docs contract tests, read-only absence checks for npm, tag, and GitHub
  release `v0.1.9`, and R1 recorded GitHub Actions run `25543373882`.
- Last validation: local validation passed; GitHub Actions run `25543373882` passed for
  `fbe8ec3`.
- Next atomic task: wait for maintainer confirmation of exactly one manual release command,
  starting with `git tag v0.1.9`.
- Known blockers: none.
- Scope guard:
  - no daemon
  - no keyboard injection
  - no LLM calls
  - no MCP server implementation
  - no app-server or desktop overlay
  - no hooks runtime
  - no UI
  - no automatic merge or release
  - no package publish
  - no GitHub release creation
  - no tag creation until separately confirmed
  - no tag deletion, tag retarget, or force-push
  - no runtime network calls

## Release Inputs

Maintainer request selected this baseline for release preparation with:

- target version: `0.1.9`
- release notes file: `docs/v0.1.9-release-notes.md`
- expected package version: `0.1.9`
- expected tag: `v0.1.9`
- release scope: npm package and GitHub release

Each actual release command still requires separate maintainer confirmation.

## Phase R0 - Release Candidate Preparation

Deliverables:

- [x] Bump package metadata to `0.1.9` without using `npm version`.
- [x] Add `docs/v0.1.9-release-notes.md`.
- [x] Update project-status docs and tests for the prepared but unreleased `v0.1.9` candidate.
- [x] Verify `v0.1.9`, npm `0.1.9`, and GitHub release `v0.1.9` do not already exist.

Acceptance:

- [x] `package.json`, `package-lock.json`, and `.codex-plugin/plugin.json` agree on `0.1.9`.
- [x] Release notes state target package version, target tag, release scope, highlights, and
  manual-only boundaries.
- [x] Project status separates published `0.1.6` from prepared but unreleased `0.1.9`.
- [x] Read-only checks show npm `0.1.9`, remote tag `v0.1.9`, and GitHub release `v0.1.9` are
  absent before release.

## Phase R1 - Evidence

Deliverables:

- [x] Run local validation.
- [x] Run package dry-run.
- [x] Checkpoint release-preparation evidence.
- [x] Commit and push the release-preparation change.
- [x] Record GitHub Actions evidence after push.

Acceptance:

- [x] `npm test` passes.
- [x] `npm run lint` passes.
- [x] `node harness/scripts/run-harness.js` passes.
- [x] `npm pack --dry-run` produces `encourage-loop-0.1.9.tgz`.
- [x] `node bin/encourage.js validate` passes.
- [x] GitHub Actions passes on the release-preparation commit.
- [x] No tag, npm publish, GitHub release, deployment, tag retarget, force-push, or destructive
  release recovery is performed.

## Test Plan

- Targeted: `node --test --test-reporter=spec tests/docs-contract.test.js tests/packaging.test.js`
- Full: `npm test`
- `npm run lint`
- `node harness/scripts/run-harness.js`
- `npm pack --dry-run`
- `node bin/encourage.js status`
- `node bin/encourage.js validate`

## Non-goals

- No npm publish.
- No tag or GitHub release.
- No tag deletion, tag retarget, or force-push.
- No daemon.
- No MCP server implementation.
- No app-server integration.
- No desktop overlay.
- No hooks runtime.
- No UI.
- No keyboard injection.
- No LLM judge or LLM calls.
- No cloud sync.
- No runtime network calls.

## Release Policy

Manual confirm. This plan prepares the `v0.1.9` release candidate, but each release command must be
confirmed separately and run one command at a time.
